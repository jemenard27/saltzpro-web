import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saltzpro.app.ios',
  appName: 'saltzpro-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
